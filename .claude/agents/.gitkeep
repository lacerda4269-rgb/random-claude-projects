# Empty at baseline. The template ships with no agents pre-placed (not even the COO).
# Residency is instantiated per project at Phase 0/1: Sophia is copied in as the default resident;
# per-project agents (P/M Leads, specialists) are added as the work requires. (Charter v6.0)
