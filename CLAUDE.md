# Random Projects — Project CLAUDE.md

**Version:** 1.0
**Project:** Random Projects (multi-mini-project container)
**Phase:** Phase 1 — open container operations
**Status:** Active

---

## Session Start — Auto Instructions

**Authoritative procedure: `Session-Start-SOP.md`** (`1. Sage's CEO Folder/6. SOPs/`). This section is the auto-load summary; the SOP governs the full sequence, fallbacks, and detail.

**At-a-glance** (query first, read second):

1. Read `Sage-project-soul.md` (`1. Sage's CEO Folder/0. Sage's CEO Project Soul/`)
2. Read latest entry in `Session Summary.md` (`1. Sage's CEO Folder/1. Session Summary/`)
3. Read `task-tracker.md` + `status-board.md` (`4. Random Projects - Live Folder/`) — the master ToDo across all mini-projects
4. Activate Sophia (COO) — opening brief + `Gotcha.md` pulse check (`2. Sophia's COO Folder/1. Operational Files/`)
5. Just-in-time reads only after that (gap-triggered)

Then confirm the current state and proceed — this project runs full auto by Jay's standing directive. **No wake-up prompt required.**

> **After a mid-session compaction:** do NOT re-run this sequence — follow `Post-Compact-Return-SOP.md` (same SOPs folder).

> **Personal notes folder:** none identified at Phase 0. If Jay creates one, record its name here and never read it.

> **Shell constraint (machine-level):** PowerShell is blocked by Bitdefender — use the Bash tool for all shell operations.

---

## What This Project Is

A standing container for multiple small, unrelated mini-projects. Each mini-project gets its own subfolder under `4. Random Projects - Live Folder/`, a scope entry in `task-tracker.md`, and a line on `status-board.md`. Lightweight ceremony by design — full MUIP launch is for real projects; anything that grows major graduates out of this container.

- Project identity and operating rules: `Sage-project-soul.md`
- Sage's global identity: `~/.claude/CLAUDE.md` (auto-loads separately)
- No P/M Lead — Sophia's Pending Changes List serves the whole project.

**Keep this file lean.** If content is tempting to add here, it belongs in `Sage-project-soul.md`.

---

## Context Window — Compact Wrap-Up & Post-Compact Protocol

**Before compaction:** When Jay signals intent to compact, Sage runs the **Compact-Wrap-Up-SOP** first — all files updated before compaction. Mid-task gate fires first: no compact while any agent is mid-task. SOP: `1. Sage's CEO Folder/6. SOPs/Compact-Wrap-Up-SOP.md`

**After any compaction:** follow `Post-Compact-Return-SOP.md` — the one mandatory read is the most recent CP section in `Session Summary.md`; do NOT re-run the full session-start sequence; re-activate Sophia before resuming.

---

*This file lives in the project root folder. Do not move it.*
*Update only when: project name, current phase, status, file paths, or session start steps change.*
